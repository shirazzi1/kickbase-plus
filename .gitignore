### Ignore Python related files
venv/
__pycache__/

### Ignore auto generated files
node_modules/
*.json
!package.json
!package-lock.json
*.log
logs/
frontend/build/

### Snapshots of the previous good copy of each data file. The names end in .last-good
### rather than .json, so the wholesale *.json rule above does not cover them.
data/

### Agent worktrees. These are checkouts of this very repository, so committing them
### would nest a repo inside itself.
.claude/

.env